<Application x:Class="GUI_assignment1_gr23.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GUI_assignment1_gr23"
             StartupUri="Views/ViewDebtorWindow.xaml">
    <Application.Resources>

        <DataTemplate x:Key="DebtTemplate">
            <WrapPanel>
                <TextBlock Text="{Binding Path=Date}"
                           Padding="5, 0"
                           MinWidth="30">
                           <!--Foreground="{Binding Path=Date}">-->
                   
                </TextBlock>
                <TextBlock Text="{Binding Path=Value}"
                           Padding="5, 0"
                           MinWidth="30">
                           <!--Foreground="{Binding Path=Date}">-->
                    
                </TextBlock>
            </WrapPanel>

        </DataTemplate>


    </Application.Resources>
</Application>
